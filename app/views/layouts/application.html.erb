<!DOCTYPE html>
<html>
<head>
  <title>Vouch</title>

  <%= javascript_include_tag :defaults, 'install-page', 'iphone-style-checkboxes' %>
  <%= stylesheet_link_tag 'screen.css', :media => 'screen, projection' %>
  <%= stylesheet_link_tag 'print.css', :media => 'print' %>
  <!--[if IE]><%= stylesheet_link_tag 'ie.css', :media => 'screen, projection' %><![endif]-->
    <script type="text/javascript">
      $(document).ready(function() {
        $('.comment-style :checkbox').iphoneStyle({ checkedLabel: 'Blog', uncheckedLabel: 'Banner' });
        $('.comment-location :checkbox').iphoneStyle({ checkedLabel: 'Top', uncheckedLabel: 'Bottom' });   
      });
  </script>
  
  <%= csrf_meta_tag %>
</head>
<body> 
<% flash.each do |key, msg| %>	
<%= content_tag :div, msg, :id => key  %>
<% end %>

<%= yield %>

</div>
</body>
</html>
